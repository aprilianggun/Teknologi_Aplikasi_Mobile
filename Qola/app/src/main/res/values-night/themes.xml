<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Qola" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your dark theme here. -->   <item name="colorButtonNormal">@color/black</item>
        <item name="colorBackgroundFloating">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardViewStyle">@color/white</item>
        <item name="cardForegroundColor">@color/white</item>
        <item name="materialCardViewStyle">@color/white</item>
    </style>

    <style name="button" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
    </style>
    <style name="MyAppTheme" parent="Theme.MaterialComponents.DayNight">
        <item name="materialCardViewStyle">@drawable/card</item>
    </style>
</resources>